# Required environment variables (set in your shell):
# - MYSQL_ROOT_PASSWORD: PostgreSQL password
# - EODHD_API_KEY: EODHD API key
# - GOOGLE_YOUTUBE_API_KEY: YouTube Data API key
# - TS_AUTHKEY: Tailscale auth key (generate at https://login.tailscale.com/admin/settings/keys)

# Optional: Database storage path on host
# If not set, defaults to ./data/postgres relative to docker-compose file
# FINALYZE_DATABASE_PATH=/path/to/your/postgres/data

# PostgreSQL settings (optional overrides)
POSTGRES_USER=dataserver
POSTGRES_DB=data_cache

# Domain (for Traefik HTTPS in production)
DOMAIN=data-server.yourdomain.com
ACME_EMAIL=your@email.com
